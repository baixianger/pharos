#!/bin/sh
set -e

if command -v systemctl >/dev/null 2>&1; then
    systemctl daemon-reload || true
    systemctl enable pharos-mesh.service >/dev/null 2>&1 || true
    systemctl restart pharos-mesh.service >/dev/null 2>&1 || true
fi

exit 0
